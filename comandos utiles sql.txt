CREATE TABLE GERENCIA 
(
  LEGAJO NUMBER NOT NULL REFERENCES EMPLEADO(LEGAJO)
, CONSTRAINT GERENCIA_PK PRIMARY KEY 
  (
    LEGAJO 
  )
  ENABLE 
);



ALTER TABLE SEGURO_NECESITA
ADD CONSTRAINT fk_INSPECCION_INSPECTOR
FOREIGN KEY (MATRICULA)
REFERENCES INSPECTOR(MATRICULA);